syntax = "proto3";
option go_package = "ChitChat/grpc/proto";

message ChatIn{
  string sender = 1;
  string text = 2;
}

message ChatOut{
  string sender = 1;
  string text = 2;
  int64 ts = 3;
}

service ChitChat {
  rpc Chat(stream ChatIn) returns (stream ChatOut);
}